apiVersion: v1
kind: Service
metadata:
  annotations: {service.beta.kubernetes.io/azure-load-balancer-internal: 'true'}
  labels: 
    fint.stack: {{ .Values.name}}
    fint.role: consumer
    {{- include "fint-environment.labels" . | nindent 4 }}

  name: consumer-{{ .Values.name}}
spec:
  type: LoadBalancer
  ports:
    - {name: '{{ .Values.consumer.service.port }}', port: {{ .Values.consumer.service.port }}}
  selector: {fint.stack: {{ .Values.name}}, fint.role: consumer}