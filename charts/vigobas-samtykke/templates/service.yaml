apiVersion: v1
kind: Service
metadata:
  name: {{ include "vigobas-samtykke.fullname" . }}
  labels:
    {{- include "vigobas-samtykke.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vigobas-samtykke.selectorLabels" . | nindent 4 }}
