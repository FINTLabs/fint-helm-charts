apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fint-altinn-ebevis-service.fullname" . }}-test-connection"
  labels:
    {{- include "fint-altinn-ebevis-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fint-altinn-ebevis-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
