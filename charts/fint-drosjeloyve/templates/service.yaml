apiVersion: v1
kind: Service
metadata:
  name: {{ include "fint-drosjeloyve.fullname" . }}
  labels:
    {{- include "fint-drosjeloyve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fint-drosjeloyve.selectorLabels" . | nindent 4 }}
