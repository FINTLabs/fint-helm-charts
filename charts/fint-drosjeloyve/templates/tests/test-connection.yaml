apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fint-drosjeloyve.fullname" . }}-test-connection"
  labels:
    {{- include "fint-drosjeloyve.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fint-drosjeloyve.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
