apiVersion: v1
kind: Service
metadata:
  name: {{ include "fint-environment.fullname" . }}
  labels:
    {{- include "fint-environment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fint-environment.selectorLabels" . | nindent 4 }}
